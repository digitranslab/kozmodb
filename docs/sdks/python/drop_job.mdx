---
title: Remove a Job
sidebarTitle: Remove a Job
---

## Description

The `drop_job()` function deletes a job from KozmoDB.

## Syntax

Use the `drop_job()` method to remove a job:

```python
project.drop_job('job_name')
```
