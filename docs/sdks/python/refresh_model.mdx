---
title: Refresh a Model
sidebarTitle: Refresh a Model
---

## Description

The `refresh()` function synchronizes the model with KozmoDB.

## Syntax

Use the `refresh()` method to refresh model's data from the KozmoDB server:

```python
model_name.refresh()
```
