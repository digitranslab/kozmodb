---
title: Refresh a Job
sidebarTitle: Refresh a Job
---

## Description

The `refresh()` function synchronizes the job with KozmoDB.

## Syntax

Use the `refresh()` method to retrieve job data from the KozmoDB server:

```python
my_job.refresh()
```
