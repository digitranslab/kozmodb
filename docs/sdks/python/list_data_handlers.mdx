---
title: List Data Handlers
sidebarTitle: List Data Handlers
---

Here is how you can fetch all available data handlers directly from Python code:

```
kozmodb = server.get_project('kozmodb')
data_handlers = kozmodb.query('SHOW HANDLERS WHERE type = \'data\'')
print(data_handlers.fetch())
```
