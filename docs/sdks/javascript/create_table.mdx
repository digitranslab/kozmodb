---
title: Create a Table
sidebarTitle: Create a Table
---

## Description

The `runQuery()` function executes a query given as its argument directly in KozmoDB.

## Syntax

Here is the syntax:

```
const query = `CREATE TABLE integration_name.table_name (SELECT * FROM data);`;
const queryResult = await KozmoDB.SQL.runQuery(query);
```
